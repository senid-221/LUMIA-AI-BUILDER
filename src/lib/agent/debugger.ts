import { nvidiaChat } from "../ai/nvidia";
import type { GeneratedFile } from "./codegen";
function extractJson(text:string){const m=text.match(/```(?:json)?\s*([\s\S]*?)\s*```/i);if(m)return m[1].trim();const a=text.indexOf("[");const b=text.lastIndexOf("]");return a>=0&&b>a?text.slice(a,b+1):text.trim();}
export async function debugFiles(files:GeneratedFile[],stderr:string,stdout:string):Promise<GeneratedFile[]>{const system=`You are Lumia AI's Debug Agent. Return ONLY valid JSON, an array of complete replacement file objects {path,language,content}. Fix the smallest necessary set using build logs. No absolute paths, .., backslashes, secrets, malware, spyware, destructive commands. If no safe fix is identifiable return [].`;const result=await nvidiaChat([{role:"system",content:system},{role:"user",content:JSON.stringify({files,stdout:stdout.slice(-12000),stderr:stderr.slice(-12000)})}],{temperature:.05,maxTokens:7000});const parsed=JSON.parse(extractJson(result.content)) as GeneratedFile[];if(!Array.isArray(parsed))throw new Error("Invalid debug response");return parsed.filter(f=>f&&typeof f.path==="string"&&typeof f.content==="string"&&!f.path.startsWith("/")&&!f.path.includes("..")&&!f.path.includes("\\")).slice(0,12);}
