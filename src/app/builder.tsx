"use client";
import { useEffect, useMemo, useState } from "react";

type User = { id: string; email: string; name: string | null };
type Project = { id: string; name: string; status: string; _count?: { files: number } };
type FileItem = { id: string; path: string; content: string; language: string | null; updatedAt?: string };
type BuildProject = { id: string; status: string; description?: string | null; files: FileItem[]; tasks: Array<{ id: string; type: string; title: string; status: string; createdAt: string; output?: unknown }> };
function language(p: string){const x=p.split(".").pop()?.toLowerCase();return x==="tsx"||x==="ts"?"typescript":x==="jsx"||x==="js"?"javascript":x==="css"?"css":x==="json"?"json":"text";}
function fileKind(path:string){return path.split(".").pop()?.toLowerCase()||"file";}
export default function Builder(){
 const [p,setP]=useState("");const [s,setS]=useState("Ready");const [r,setR]=useState<any>(null);const [user,setUser]=useState<User|null>(null);const [projects,setProjects]=useState<Project[]>([]);const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [name,setName]=useState("");const [mode,setMode]=useState<"signin"|"signup">("signin");const [showAuth,setShowAuth]=useState(false);const [authStep,setAuthStep]=useState<"choices"|"form">("choices");const [showPassword,setShowPassword]=useState(false);const [selected,setSelected]=useState("");const [files,setFiles]=useState<FileItem[]>([]);const [active,setActive]=useState<FileItem|null>(null);const [code,setCode]=useState("");const [saving,setSaving]=useState(false);const [fixing,setFixing]=useState(false);const [repo,setRepo]=useState("");const [branch,setBranch]=useState("main");const [token,setToken]=useState("");const [syncing,setSyncing]=useState(false);const [deploying,setDeploying]=useState(false);const [view,setView]=useState<"editor"|"preview">("editor");const [sidebarOpen,setSidebarOpen]=useState(false);const [building,setBuilding]=useState(false);const [buildProject,setBuildProject]=useState<BuildProject|null>(null);const [activeBuildFile,setActiveBuildFile]=useState("");const [lastSig,setLastSig]=useState("");
 async function load(){try{const me=await fetch("/api/auth/me").then(x=>x.json());setUser(me.user);if(me.user){const data=await fetch("/api/projects").then(x=>x.json());setProjects(data.projects||[]);if(!building)setS("Ready");}else setS("");}catch{setS("Unable to load session");}}
 useEffect(()=>{load();},[]);
 async function refreshBuild(id:string){const x=await fetch(`/api/builder/events?projectId=${encodeURIComponent(id)}`,{cache:"no-store"});const d=await x.json();if(!x.ok||!d.ok)throw Error(d.error||"Unable to read build progress");return d.project as BuildProject;}
 useEffect(()=>{if(!building||!selected)return;let stop=false;const poll=async()=>{try{const project=await refreshBuild(selected);if(stop)return;setBuildProject(project);const all=project.files||[];const sig=all.map(f=>`${f.path}|${f.content?.length||0}|${f.updatedAt||""}`).join(";");if(sig!==lastSig){setLastSig(sig);const newest=all[all.length-1];if(newest){setActiveBuildFile(newest.path);setActive(newest);setCode(newest.content||"");setView("editor");}}
 if(project.status==="BUILDING"||project.status==="PLANNING"){setS(all.length?`Building · ${all.length} files generated`:project.status==="PLANNING"?"Planning build…":"Generating files…");return;}
 if(project.status==="READY"){setBuilding(false);setFiles(all);const first=all[0];if(first){setActiveBuildFile(first.path);setActive(first);setCode(first.content||"");}setS(`Build ready · ${all.length} files`);setR({projectId:selected,status:"READY",project});await load();return;}
 if(project.status==="FAILED"){setBuilding(false);const desc=String(project.description||"");setS(desc.includes("\n\nBuild error: ")?desc.split("\n\nBuild error: ").slice(1).join("\n\nBuild error: "):"Build failed.");await load();}}catch(e){if(!stop)setS(e instanceof Error?e.message:"Unable to read build progress");}};poll();const t=window.setInterval(poll,1000);return()=>{stop=true;window.clearInterval(t);};},[building,selected,lastSig]);
 async function auth(){setS(mode==="signin"?"Signing in…":"Creating account…");const url=mode==="signin"?"/api/auth/signin":"/api/auth/signup";const body=mode==="signin"?{email,password}:{email,password,name};const x=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await x.json();if(!x.ok)throw Error(d.error||"Authentication failed");setUser(d.user);setEmail("");setPassword("");setName("");setShowAuth(false);setS("Ready");const list=await fetch("/api/projects").then(v=>v.json());setProjects(list.projects||[]);}
 async function openProject(id:string,showStatus=true){if(!id){setSelected("");setFiles([]);setActive(null);setCode("");setBuildProject(null);return;}setSelected(id);if(showStatus)setS("Loading project…");const x=await fetch(`/api/projects/${id}/files`);const d=await x.json();if(!x.ok)throw Error(d.error||"Unable to load files");setFiles(d.files||[]);setActive(null);setCode("");setBuildProject(null);if(showStatus)setS("Ready");setView("editor");setSidebarOpen(false);}
 function selectFile(path:string){const f=files.find(x=>x.path===path)||null;setActive(f);setCode(f?.content||"");setActiveBuildFile(path);setView("editor");setSidebarOpen(false);}
 async function save(){if(!selected||!active)return;setSaving(true);setS("Saving…");try{const x=await fetch(`/api/projects/${selected}/files`,{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({path:active.path,content:code,language:language(active.path)})});const d=await x.json();if(!x.ok)throw Error(d.error||"Save failed");setFiles(v=>v.map(f=>f.path===active.path?{...f,content:code}:f));setActive(v=>v?{...v,content:code}:v);setS("Saved");}catch(e){setS(e instanceof Error?e.message:"Save failed");}finally{setSaving(false);}}
 async function aiFix(){if(!active)return;setFixing(true);setS("AI is fixing the file…");try{const x=await fetch("/api/builder/debug",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({projectId:selected,files:[{path:active.path,content:code}],stderr:"User requested an AI fix for this file."})});const d=await x.json();if(!x.ok||!d.ok)throw Error(d.error||"AI Fix failed");const rep=d.replacements?.find((f:any)=>f.path===active.path);if(rep){setCode(rep.content);setS("AI fix generated — review and Save");}else setS("No replacement returned");}catch(e){setS(e instanceof Error?e.message:"AI Fix failed");}finally{setFixing(false);}}
 async function build(){if(!p.trim()||building)return;setBuilding(true);setS("Starting Lumia AI build…");setBuildProject(null);setLastSig("");setFiles([]);setActive(null);setCode("");try{const x=await fetch("/api/builder/build",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({prompt:p,projectId:selected||undefined})});const d=await x.json();if(!x.ok||!d.ok)throw Error(d.error||"Unable to start build");setR(d);setSelected(d.projectId);setS("Build started · Lumia AI is working…");}catch(e){setBuilding(false);setS(e instanceof Error?e.message:"Build failed");}}
 async function syncGitHub(){if(!selected||!repo||!token){setS("Enter repository and GitHub token");return;}setSyncing(true);setS("Syncing to GitHub…");try{const current=active?files.map(f=>f.path===active.path?{...f,content:code}:f):files;const x=await fetch("/api/github/sync",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({projectId:selected,repository:repo,branch,token,message:`Update ${projects.find(x=>x.id===selected)?.name||"project"} from Lumia AI`,files:current.map(f=>({path:f.path,content:f.content}))})});const d=await x.json();if(!x.ok||!d.ok)throw Error(d.error||"GitHub sync failed");setToken("");setS(`GitHub synced · ${d.branch}`);}catch(e){setS(e instanceof Error?e.message:"GitHub sync failed");}finally{setSyncing(false);}}
 async function deploy(){if(!selected)return;setDeploying(true);setS("Deploying to Vercel…");try{const x=await fetch(`/api/projects/${selected}/deploy`,{method:"POST"});const d=await x.json();if(!x.ok||!d.ok)throw Error(d.error||"Deployment failed");setS("Deployment queued on Vercel");}catch(e){setS(e instanceof Error?e.message:"Deployment failed");}finally{setDeploying(false);}}
 async function signout(){await fetch("/api/auth/signout",{method:"POST"});setUser(null);setProjects([]);setFiles([]);setActive(null);setSelected("");setS("");}
 const grouped=useMemo(()=>files.map(f=>f.path),[files]);
 if(!user)return <main className="landing"><div className="landing-inner"><div className="mark">LA</div><h1 className="brand">LUMIA BUILDER</h1><p className="copy">Describe what you want to build and Lumia AI turns it into a project.</p><button className="login" onClick={()=>{setShowAuth(v=>!v);setAuthStep("choices");setS("")}}>Get Started</button>{showAuth&&authStep==='choices'&&<div className="choices"><button className="choice primary" onClick={()=>{setMode('signup');setAuthStep('form')}}>Create an account</button><button className="choice" onClick={()=>{setMode('signin');setAuthStep('form')}}>Sign in</button></div>}{showAuth&&authStep==='form'&&<div className="auth"><h2>{mode==='signin'?'Sign in':'Create an account'}</h2><p>{mode==='signin'?'Enter your details to continue.':'Create your Lumia Builder account.'}</p>{mode==='signup'&&<input value={name} onChange={e=>setName(e.target.value)} placeholder="Name"/>}<input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" type="email"/><div className="password"><input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (8+ characters)" type={showPassword?'text':'password'}/><button className="show" type="button" onClick={()=>setShowPassword(v=>!v)}>{showPassword?'Hide':'Show'}</button></div><button className="submit" onClick={()=>auth().catch(e=>setS(e.message))}>{mode==='signin'?'Sign in':'Create account'}</button><button className="back" onClick={()=>{setAuthStep('choices');setS('')}}>Back</button>{s&&<p className="err">{s}</p>}</div>}<div className="foot">LUMIA AI BUILDER</div></div><style>{`*{box-sizing:border-box}.landing{min-height:100vh;background:linear-gradient(180deg,#fafaff,#fff);color:#111827;font-family:Inter,system-ui,sans-serif;display:grid;place-items:center;padding:24px}.landing-inner{width:min(100%,410px);text-align:center}.mark{width:78px;height:78px;margin:0 auto 18px;border-radius:24px;display:grid;place-items:center;background:linear-gradient(135deg,#7c3aed,#2563eb);color:#fff;font-weight:900;font-size:27px;box-shadow:0 18px 42px rgba(79,70,229,.22)}.brand{font-size:30px;font-weight:900;letter-spacing:-1px;margin:0 0 9px}.copy{margin:0 auto 28px;color:#64748b;font-size:14px;line-height:1.55;max-width:340px}.login{width:100%;height:52px;border:0;border-radius:15px;background:linear-gradient(100deg,#7c3aed,#2563eb);color:#fff;font-weight:800;font-size:15px}.choices{display:grid;gap:10px;margin-top:12px}.choice{height:49px;border-radius:14px;border:1px solid #ddd6fe;background:#fff;color:#5b21b6;font-weight:750}.choice.primary{background:linear-gradient(100deg,#7c3aed,#2563eb);color:#fff;border:0}.auth{margin-top:14px;padding:20px;background:#fff;border:1px solid #e7e9ef;border-radius:20px;box-shadow:0 18px 55px rgba(15,23,42,.09);text-align:left}.auth input{width:100%;height:48px;border:1px solid #dfe3eb;border-radius:12px;padding:0 12px;margin-bottom:10px;font-size:14px;outline:none}.auth h2{margin:0 0 5px}.auth p{color:#64748b;font-size:13px;margin:0 0 14px}.password{position:relative}.password input{padding-right:60px}.show{position:absolute;right:5px;top:5px;height:38px;border:0;background:none;color:#6d28d9;font-weight:700}.submit{width:100%;height:46px;border:0;border-radius:12px;background:#111827;color:#fff;font-weight:800}.back{width:100%;height:42px;border:0;border-radius:12px;background:#f8fafc;color:#64748b;margin-top:8px}.err{color:#dc2626!important;text-align:center}.foot{margin-top:28px;color:#9aa3b5;font-size:11px}`}</style></main>;
 return <main className="app"><style>{`*{box-sizing:border-box}.app{min-height:100vh;background:#f5f6fa;color:#111827;font-family:Inter,system-ui,sans-serif}.top{height:68px;background:rgba(255,255,255,.97);border-bottom:1px solid #e7e9ef;display:flex;align-items:center;justify-content:space-between;padding:0 20px;position:sticky;top:0;z-index:60;backdrop-filter:blur(12px)}.brandline{display:flex;align-items:center;gap:10px}.mini{width:36px;height:36px;border-radius:11px;background:linear-gradient(135deg,#7c3aed,#2563eb);display:grid;place-items:center;color:#fff;font-weight:900;font-size:12px}.btitle{font-weight:850;font-size:15px}.bsub{font-size:11px;color:#94a3b8;margin-left:6px}.userline{display:flex;align-items:center;gap:8px;color:#667085;font-size:12px}.signout{border:1px solid #e1e5eb;background:#fff;border-radius:10px;padding:8px 11px;color:#374151}.toggle{display:none;border:1px solid #e1e5eb;background:#fff;border-radius:10px;width:40px;height:40px}.shell{max-width:1500px;margin:auto;padding:16px;display:grid;grid-template-columns:245px minmax(0,1fr);gap:16px}.side{background:#fff;border:1px solid #e5e7eb;border-radius:16px;padding:13px;height:calc(100vh - 100px);position:sticky;top:84px;overflow:auto;box-shadow:0 10px 28px rgba(15,23,42,.04)}.label{font-size:10px;font-weight:850;color:#8b93a6;text-transform:uppercase;letter-spacing:.08em;margin:5px 0 8px}.select{width:100%;height:42px;border:1px solid #dfe3eb;border-radius:10px;background:#fff;padding:0 10px}.files{margin-top:9px}.file{display:flex;align-items:center;gap:8px;width:100%;padding:9px;border:0;border-radius:9px;background:transparent;text-align:left;color:#475569;margin-bottom:3px;font-size:12px}.file:hover{background:#f7f8fc}.file.active{background:#f0ebff;color:#5b21b6;font-weight:750}.ext{width:30px;height:20px;border-radius:5px;background:#f1f5f9;color:#64748b;display:grid;place-items:center;font-size:8px;font-weight:800;text-transform:uppercase;flex:0 0 auto}.file.active .ext{background:#e2d9ff;color:#6d28d9}.empty{color:#9aa3b5;font-size:12px;line-height:1.5;padding:10px 4px}.main{min-width:0}.hero{background:#fff;border:1px solid #e5e7eb;border-radius:16px;padding:20px;box-shadow:0 10px 28px rgba(15,23,42,.04)}.hero h1{font-size:25px;letter-spacing:-.7px;margin:0 0 6px}.hero p{margin:0;color:#667085;font-size:13px}.prompt{display:flex;gap:10px;margin-top:16px}.prompt textarea{flex:1;min-height:92px;resize:vertical;border:1px solid #dfe3eb;border-radius:13px;padding:13px;font:inherit;outline:none}.build{align-self:stretch;min-width:132px;border:0;border-radius:13px;background:linear-gradient(100deg,#7c3aed,#2563eb);color:#fff;font-weight:850}.build:disabled{opacity:.6}.status{margin-top:10px;color:#667085;font-size:12px;min-height:18px}.build-progress{margin-top:16px;background:#fff;border:1px solid #e5e7eb;border-radius:16px;overflow:hidden}.progress-head{height:51px;border-bottom:1px solid #edf0f4;display:flex;align-items:center;justify-content:space-between;padding:0 14px}.progress-title{font-size:12px;font-weight:800}.progress-count{font-size:11px;color:#7c3aed}.progress-layout{display:grid;grid-template-columns:285px minmax(0,1fr);min-height:520px}.build-files{border-right:1px solid #edf0f4;background:#fbfbfd;padding:10px;overflow:auto}.build-file{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:9px;border-radius:9px;font-size:11px;color:#475569;background:transparent;border:0;width:100%;text-align:left}.build-file:hover,.build-file.on{background:#f0ebff;color:#5b21b6}.build-file-left{display:flex;align-items:center;gap:8px;min-width:0}.build-file-name{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.write-state{font-size:9px;color:#94a3b8;white-space:nowrap}.write-state.done{color:#16a34a}.live-code{background:#0b1020;color:#dbe4ff;display:flex;flex-direction:column;min-width:0}.live-code-head{height:42px;border-bottom:1px solid rgba(255,255,255,.08);display:flex;align-items:center;justify-content:space-between;padding:0 13px;font-size:11px;color:#a8b3c7}.live-code pre{margin:0;padding:16px;overflow:auto;flex:1;font:12px/1.65 ui-monospace,SFMono-Regular,Menlo,monospace;white-space:pre-wrap;word-break:break-word}.cursor{display:inline-block;width:7px;height:15px;background:#a78bfa;vertical-align:-2px;margin-left:2px;animation:blink .9s steps(1) infinite}@keyframes blink{50%{opacity:0}}.workspace{margin-top:16px;background:#fff;border:1px solid #e5e7eb;border-radius:16px;overflow:hidden;box-shadow:0 10px 28px rgba(15,23,42,.04)}.bar{height:52px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #edf0f4;padding:0 14px}.tabs{display:flex;gap:4px}.tab{border:0;background:#f8fafc;border-radius:9px;padding:8px 11px;color:#64748b}.tab.on{background:#eee8ff;color:#5b21b6;font-weight:750}.actions{display:flex;gap:7px}.act{border:1px solid #e1e5eb;background:#fff;border-radius:9px;padding:8px 10px;font-size:12px;color:#374151}.act:disabled{opacity:.5}.editor{display:grid;grid-template-columns:230px 1fr;min-height:500px}.tree{border-right:1px solid #edf0f4;padding:10px;overflow:auto}.pane{min-width:0}.code{width:100%;height:500px;border:0;resize:none;padding:16px;font:13px/1.6 ui-monospace,SFMono-Regular,Menlo,monospace;outline:none;background:#0b1020;color:#e5e7eb}.preview{padding:18px;min-height:500px}.previewbox{min-height:450px;border:1px dashed #cfd5df;border-radius:14px;background:#fafbfe;padding:18px;white-space:pre-wrap;color:#475569;font-size:13px}.meta{padding:14px;border-top:1px solid #edf0f4;color:#667085;font-size:12px}@media(max-width:860px){.shell{grid-template-columns:1fr;padding:12px}.side{display:none;position:fixed;left:12px;right:12px;top:80px;height:calc(100vh - 92px);z-index:50}.side.open{display:block}.toggle{display:block}.progress-layout{grid-template-columns:1fr}.build-files{border-right:0;border-bottom:1px solid #edf0f4;max-height:230px}.editor{grid-template-columns:1fr}.tree{display:none}.prompt{flex-direction:column}.build{min-height:48px}.bsub{display:none}.top{padding:0 12px}.workspace{margin-top:12px}}`}</style><header className="top"><div className="brandline"><button className="toggle" onClick={()=>setSidebarOpen(v=>!v)}>☰</button><div className="mini">LA</div><div><span className="btitle">Lumia AI Builder</span><span className="bsub">Autonomous software builder</span></div></div><div className="userline"><span>{user.name||user.email}</span><button className="signout" onClick={signout}>Sign out</button></div></header><section className="shell"><aside className={`side ${sidebarOpen?'open':''}`}><div className="label">Projects</div><select className="select" value={selected} onChange={e=>openProject(e.target.value)}><option value="">New project</option>{projects.map(x=><option key={x.id} value={x.id}>{x.name} · {x.status}</option>)}</select><div className="label" style={{marginTop:18}}>Files</div><div className="files">{grouped.length?grouped.map(path=><button key={path} className={`file ${active?.path===path?'active':''}`} onClick={()=>selectFile(path)}><span className="ext">{fileKind(path)}</span><span>{path}</span></button>):<div className="empty">Project files will appear here.</div>}</div></aside><div className="main"><section className="hero"><h1>Build with Lumia AI</h1><p>Describe the software you want. Watch every file appear and see its code while Lumia writes it.</p><div className="prompt"><textarea value={p} onChange={e=>setP(e.target.value)} placeholder="e.g. Build a modern SaaS landing page with navbar, pricing and responsive mobile design."/><button className="build" onClick={build} disabled={building}>{building?'Building…':'Build'}</button></div><div className="status">{s}</div></section>{building&&<section className="build-progress"><div className="progress-head"><div className="progress-title">Lumia AI Builder · Live generation</div><div className="progress-count">{buildProject?.files?.length||0} files</div></div><div className="progress-layout"><div className="build-files">{(buildProject?.files||[]).map(f=><button key={f.path} className={`build-file ${activeBuildFile===f.path?'on':''}`} onClick={()=>{setActiveBuildFile(f.path);setActive(f);setCode(f.content||'')}}><span className="build-file-left"><span className="ext">{fileKind(f.path)}</span><span className="build-file-name">{f.path}</span></span><span className={`write-state ${f.content?'done':''}`}>{f.content?'✓':'writing'}</span></button>)}{!(buildProject?.files||[]).length&&<div className="empty">Waiting for Lumia to generate the first file…</div>}</div><div className="live-code"><div className="live-code-head"><span>{activeBuildFile||'No file yet'}</span><span>{activeBuildFile?language(activeBuildFile):'Lumia engine'}</span></div><pre>{activeBuildFile?(buildProject?.files||[]).find(f=>f.path===activeBuildFile)?.content||code||'':''}<span className="cursor"/></pre></div></div></section>}<section className="workspace"><div className="bar"><div className="tabs"><button className={`tab ${view==='preview'?'on':''}`} onClick={()=>setView('preview')}>Preview</button><button className={`tab ${view==='editor'?'on':''}`} onClick={()=>setView('editor')}>Code</button></div><div className="actions"><button className="act" onClick={save} disabled={!active||saving}>Save</button><button className="act" onClick={aiFix} disabled={!active||fixing}>AI Fix</button><button className="act" onClick={deploy} disabled={!selected||deploying}>Deploy</button></div></div>{view==='editor'&&<div className="editor"><div className="tree">{grouped.map(path=><button key={path} className={`file ${active?.path===path?'active':''}`} onClick={()=>selectFile(path)}><span className="ext">{fileKind(path)}</span><span>{path}</span></button>)}{!grouped.length&&<div className="empty">Generated files will appear here.</div>}</div><div className="pane"><textarea className="code" value={code} onChange={e=>setCode(e.target.value)} placeholder="Select a generated file to view or edit its code."/></div></div>}{view==='preview'&&<div className="preview"><div className="previewbox">{selected?`Project: ${projects.find(x=>x.id===selected)?.name||"Lumia Project"}\n\n${files.length?`${files.length} files are available. Select Code to inspect them.` :'No generated files yet.'}`:'Start a build to generate your application.'}</div></div>}<div className="meta">{selected?`Project ${selected}`:'No project selected'}</div></section></div></section></main>;
}
