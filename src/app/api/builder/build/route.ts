import {NextResponse} from "next/server";
import {planBuild} from "@/lib/agent/orchestrator";
import {autonomousBuild} from "@/lib/agent/build-loop";
import {getCurrentUser} from "@/lib/auth";
import {prisma} from "@/lib/prisma";
export const maxDuration=300;
export async function POST(request:Request){try{const user=await getCurrentUser(); if(!user)return NextResponse.json({ok:false,error:"Please sign in first."},{status:401}); const body=await request.json(); const prompt=typeof body?.prompt==="string"?body.prompt.trim():""; if(!prompt)return NextResponse.json({ok:false,error:"A build request is required."},{status:400}); let projectId=typeof body?.projectId==="string"?body.projectId:null; if(projectId){const owned=await prisma.builderProject.findFirst({where:{id:projectId,ownerId:user.id},select:{id:true}}); if(!owned)return NextResponse.json({ok:false,error:"Project not found."},{status:404})}else{const name=prompt.split(/\s+/).slice(0,6).join(" ").replace(/[^a-zA-Z0-9 _-]/g,"").trim()||"Lumia Project"; const project=await prisma.builderProject.create({data:{ownerId:user.id,name,description:prompt,status:"PLANNING"}}); projectId=project.id} await prisma.builderProject.update({where:{id:projectId!},data:{status:"BUILDING",description:prompt}}); const planned=await planBuild(prompt); const built=await autonomousBuild(prompt,planned.plan); if(built.files?.length){await prisma.$transaction(built.files.map((file:any)=>prisma.builderFile.upsert({where:{projectId_path:{projectId:projectId!,path:file.path}},create:{projectId:projectId!,path:file.path,content:file.content,language:file.language??null},update:{content:file.content,language:file.language??null}})))} await prisma.builderTask.create({data:{projectId:projectId!,type:"PLAN",title:"Build request",status:built.result?.status==="PASSED"?"DONE":"FAILED",input:{prompt},output:{plan:planned.plan,retries:built.retries}}}); await prisma.builderProject.update({where:{id:projectId!},data:{status:built.result?.status==="PASSED"?"READY":"FAILED"}}); return NextResponse.json({ok:true,projectId,plan:planned.plan,...built});}catch(error){console.error(error);return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Autonomous build failed."},{status:500})}}
