import {NextResponse} from "next/server";
import {prisma} from "@/lib/prisma";
import {createSession,hashPassword} from "@/lib/auth";
export async function POST(req:Request){try{const {email,password,name}=await req.json(); const normalized=String(email||"").trim().toLowerCase(); if(!/^\S+@\S+\.\S+$/.test(normalized)||String(password||"").length<8)return NextResponse.json({ok:false,error:"Valid email and password of at least 8 characters are required"},{status:400}); const exists=await prisma.user.findUnique({where:{email:normalized}}); if(exists)return NextResponse.json({ok:false,error:"Email already registered"},{status:409}); const user=await prisma.user.create({data:{email:normalized,passwordHash:hashPassword(String(password)),name:String(name||"").trim()||null}}); await createSession(user.id); return NextResponse.json({ok:true,user:{id:user.id,email:user.email,name:user.name}})}catch(e){console.error(e);return NextResponse.json({ok:false,error:"Signup failed"},{status:500})}}
