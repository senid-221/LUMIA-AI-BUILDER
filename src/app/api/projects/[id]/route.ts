import {NextResponse} from "next/server";
import {prisma} from "@/lib/prisma";
import {getCurrentUser} from "@/lib/auth";
export async function GET(_req:Request,{params}:{params:Promise<{id:string}>}){const user=await getCurrentUser(); if(!user)return NextResponse.json({ok:false,error:"Unauthorized"},{status:401}); const {id}=await params; const project=await prisma.builderProject.findFirst({where:{id,ownerId:user.id},include:{files:true,tasks:{orderBy:{createdAt:"desc"}},executions:{orderBy:{createdAt:"desc"},take:20}}}); if(!project)return NextResponse.json({ok:false,error:"Project not found"},{status:404}); return NextResponse.json({ok:true,project})}
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){const user=await getCurrentUser(); if(!user)return NextResponse.json({ok:false,error:"Unauthorized"},{status:401}); const {id}=await params; const existing=await prisma.builderProject.findFirst({where:{id,ownerId:user.id}}); if(!existing)return NextResponse.json({ok:false,error:"Project not found"},{status:404}); try{const body=await req.json(); const data:any={}; if(body.name!==undefined)data.name=String(body.name).trim(); if(body.description!==undefined)data.description=String(body.description||"").trim()||null; if(body.status!==undefined)data.status=body.status; if(body.stack!==undefined)data.stack=body.stack; const project=await prisma.builderProject.update({where:{id},data}); return NextResponse.json({ok:true,project})}catch(e){console.error(e);return NextResponse.json({ok:false,error:"Project update failed"},{status:400})}}
export async function DELETE(_req:Request,{params}:{params:Promise<{id:string}>}){const user=await getCurrentUser(); if(!user)return NextResponse.json({ok:false,error:"Unauthorized"},{status:401}); const {id}=await params; const result=await prisma.builderProject.deleteMany({where:{id,ownerId:user.id}}); if(!result.count)return NextResponse.json({ok:false,error:"Project not found"},{status:404}); return NextResponse.json({ok:true})}
